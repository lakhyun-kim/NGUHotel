<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="kr.hotel.dao.gpack.GpackMapper">
	<select id="gpList" parameterType="map" resultType="gpackCommand">
		SELECT
		  *
		FROM 
		  h_gpackage 	
	</select>
	
	<select id="getRowCount" parameterType="map" resultType="integer">
		SELECT
		   count(*)
		FROM h_gpackage
	</select>

</mapper>